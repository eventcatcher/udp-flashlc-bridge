## Unknown Message Fields

The LocalConnection message header contains a fixed sequence of 31 bytes.

Their exact purpose is currently unknown.

Experiments show that Flash requires these bytes to be present for successful message delivery.
